# AzCLI, Kubectl, Terraform (and other Hashcorp clis), Calicoctl, vim, dnsutils, ping

This Dockerfile installs all that, uses a non-root, non-sudoable setup and tries to still have a minimal image size.